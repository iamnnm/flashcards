<!DOCTYPE html>
<html>
  <head>
    <title><%= t '.title' %></title>

    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js" integrity="sha384-ka7Sk0Gln4gmtz2MlQnikT1wXgYsOg+OMhuP+IlRH9sENBO0LRn5q+8nbTov4+1p" crossorigin="anonymous"></script>
  </head>
  <body>
    <section class="container">
      <header class="d-flex flex-wrap justify-content-center py-3 mb-4 border-bottom">
        <a href="/" class="d-flex align-items-center mb-3 mb-md-0 me-md-auto text-dark text-decoration-none">
          <span><h1><%= t '.logo' %></h1></span>
        </a>
        <ul class="nav nav-pills">
          <li class="nav-item d-flex align-items-center mb-3 mb-md-0 me-md-auto ms-2">
            <%= link_to t('layouts.application.button_all_cards'), cards_path, class: 'btn btn-success'%>
          </li>
          <li class="nav-item d-flex align-items-center mb-3 mb-md-0 me-md-auto ms-2">
            <%= link_to t('layouts.application.button_add_cards'), new_card_path, class: 'btn btn-warning'%>
          </li>
        </ul>
      </header>
      <% flash.each do |type, msg| %>
        <div class="alert alert-info text-center">
          <%= msg %>
        </div>
      <% end %>

      <%= yield %>
    </section>
  </body>
</html>
